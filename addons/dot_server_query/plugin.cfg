[plugin]

name="dot-server-query"
description="Server queries: the dot query protocol (DQP) with challenges, sections, conditional polling and WebSocket, plus full A2S for the trackers that speak nothing else. Both answer from one snapshot, on one socket or two."
author="dot"
version="0.1.0"
script="dot_server_query_plugin.gd"
